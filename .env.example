# Copy to .env.local and set this to your webhook endpoint (GoHighLevel
# inbound webhook, a Google Apps Script bound to a Sheet, Zapier, etc.) to
# have the appointment form POST leads there. Leave unset during development
# — submissions will just be logged to the server console.
NEXT_PUBLIC_LEAD_WEBHOOK_URL=
